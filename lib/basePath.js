// The path prefix the whole app is served under. Must match `basePath` in
// next.config.js. next/link, next/image and next/navigation prefix this
// automatically; plain fetch() and <form action> do not, so use this constant
// for those.
export const BASE_PATH = '/bdb';
